apiVersion: v1
kind: Service
metadata:
  name: postgresql-nodeport
  namespace: postgresql
spec:
  type: NodePort
  selector:
    cnpg.io/cluster: postgresql
    role: primary
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30432
