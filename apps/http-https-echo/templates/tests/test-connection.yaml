apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "http-https-echo.fullname" . }}-test-connection"
  labels:
    {{- include "http-https-echo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "http-https-echo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
